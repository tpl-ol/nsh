// 命令: --壳
import { B } from "../../b/t";
import 取区域 from "../../locale/mod";

async function 壳(b: B, 参数: Array<string>): Promise<number> {
  const 区 = 取区域();

  // TODO

  return 0;
}

export default 壳;
